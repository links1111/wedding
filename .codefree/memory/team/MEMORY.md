# Memory Index
## project — audio-playback-policy
婚礼请柬系统音乐播放策略：浏览器自动播放限制下，禁止在用户无交互时自动播放有声音频。用户交互触发：每次用户点击页面任意位置（除音乐按钮和回执表单区域外）均尝试恢复有声播放。引入 musicClosed 状态区分"用户主动关闭音乐"与"暂停"：当用户点击音乐按钮关闭音乐后 musicClosed 置为...
## project — carousel-switch-fix
婚礼请柬系统轮播图切换修复方案： 根本原因：performFadeSwitch 过渡完成后，所有新图片的 inline style opacity 被设为 '1'，覆盖了 CSS .slide { opacity: 0 } 和 .slide.active { opacity: 1 } 的样式控制，导...
## project — subcard-design
婚礼请柬系统子卡片视觉设计规范：8 种卡片类型需有独立的视觉装饰和样式，避免简单重复。相遇（meet）：星形徽记，标题前缀装饰。心动（heartbeat）：双层边框，增强阴影。约会（date）：花朵徽记，斜体日期。表白（confess）：笔形徽记，斜体标题。在一起（together）：无限符号徽记，...
## project — color-scheme
婚礼请柬系统配色规范（莫兰迪粉 + 灰蓝绿法式轻奢风格）：主色调干枯玫瑰粉 (#d4a5a5)，温柔高级；辅助色灰蓝绿 (#84a59d)，沉静优雅；背景色暖灰白 (#faf7f5)，柔和不刺眼；文字深灰褐 (#4a403a)，温暖稳重；点缀香槟金 (#c9a96e)，精致点睛。禁止使用灰色、纯红等...
## project — settings-preview-logic
婚礼请柬系统设置页面预览功能逻辑： - 主请柬编辑时：左侧预览栏显示主请柬完整预览（调用 renderMainCardPreview），右侧显示主请柬配置表单。 - 子卡片编辑时：左侧预览栏显示对应子卡片预览（调用 renderCardPreview），右侧显示子卡片编辑表单。 - 预览栏显示/隐藏...
## feedback — careful-before-edit
用户要求：每次修改代码后必须 git commit。修改前务必仔细排查问题根因，完整阅读相关代码和布局层级后再动手，一次性修改正确，强调错误修改会导致严重事故，不要急于修改。修改后需自行验证逻辑正确性，不能只改不验。用户对"桌面端空间有限"这类错误假设非常不满，要求认真对待用户的明确指令（如"保持和...
## project — settings-layout
婚礼请柬系统设置页面布局与交互规范：采用左右分栏结构。左侧为时间轴样式卡片列表（宽度 220px），垂直渐变金线作为时间轴主干，节点为圆点，主请柬节点更大更亮，选中节点高亮发光，节点右侧显示卡片标题和类型标签，每个子卡片节点右侧放置上移（↑）、下移（↓）、删除（×）图标按钮，无文字。右侧内容区分为上...
## project — carousel-smooth-switch
婚礼请柬系统轮播图切换规范：平滑交叉淡入淡出，滚动到新卡片时旧图片淡出的同时新图片第一张淡入，不等待预加载完成，过渡时间 0.8 秒。预加载机制：切换前需后台预加载新卡片的图片列表，确保后续轮播流畅。自动播放：切换后轮播图需自动继续播放，定时器在淡入淡出完成后重新启动。实现注意事项：performF...
## project — active-initiative
婚礼请柬系统当前工作流与待办事项：编辑页面和管理页面右上角的"查看请柬"按钮已修复跳转逻辑：管理后台通过 /admin/{admin_token} 访问，必须从 /api/a/:token/meta 接口返回数据中的 token 字段获取真正的请柬 token，并构建跳转链接 /w/{WEDDING...