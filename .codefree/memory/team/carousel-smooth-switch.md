---
name: carousel-smooth-switch
type: project
scope: team
description: 婚礼请柬系统轮播图平滑切换机制：当用户滚动到不同卡片时，轮播图不再立即重置，而是先预加载新图片列表，等待当前图片完整显示（约 2 秒缓冲期）且所有新图片加载完成后，再平滑替换图片列表。切换过程采用交叉淡入淡出效果：旧图片淡出（0.8 秒）的同时新图片淡入（0.8 秒），实现无缝过渡。保持原有轮播逻辑...
created: "2026-09-08T00:59:32.036Z"
updated: "2026-09-08T03:16:03.379Z"
---
婚礼请柬系统轮播图平滑切换机制：当用户滚动到不同卡片时，轮播图不再立即重置，而是先预加载新图片列表，等待当前图片完整显示（约 2 秒缓冲期）且所有新图片加载完成后，再平滑替换图片列表。切换过程采用交叉淡入淡出效果：旧图片淡出（0.8 秒）的同时新图片淡入（0.8 秒），实现无缝过渡。保持原有轮播逻辑不变，只在切换时机和过渡效果上做优化。